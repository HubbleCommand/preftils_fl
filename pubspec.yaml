name: preftils
description: "A new Flutter package project."
version: 0.0.1
homepage: "https://github.com/HubbleCommand/preftils_fl"

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
